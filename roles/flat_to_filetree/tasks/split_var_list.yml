---
# This task is included from main.yml and loops over var_to_folder_map

- name: Set processing variables for {{ map_item.key }}
  ansible.builtin.set_fact:
    target_var_name: "{{ map_item.key }}"
    target_folder_name: "{{ map_item.value }}"
    # Look up the var in our safe, combined fact
    target_var_value: "{{ loaded_vars_combined[map_item.key] | default(None) }}"
  run_once: true
  delegate_to: localhost

- name: "Initialize file list for this variable"
  ansible.builtin.set_fact:
    __files_to_create: []
  run_once: true
  delegate_to: localhost

- name: "Get processing rules for {{ target_var_name }}"
  ansible.builtin.set_fact:
    item_rules: "{{ flat_to_filetree_filename_logic[target_var_name] | default(flat_to_filetree_filename_logic['__default__']) }}"
    primary_rule: "{{ (flat_to_filetree_filename_logic[target_var_name] | default(flat_to_filetree_filename_logic['__default__']))[0] }}"
  run_once: true
  delegate_to: localhost

# Branch 1: Handle Dictionaries (like gateway_settings)
- name: "Process 'dict' variable"
  ansible.builtin.set_fact:
    __files_to_create:
      - subfolder: "{{ (primary_rule.subfolder | default('') | trim | length > 0) | ternary(primary_rule.subfolder | safe_path_segment, '') }}"
        filename: "{{ primary_rule.filename | safe_path_segment }}"
        content: "{{ {target_var_name: target_var_value} | to_nice_yaml(indent=2, sort_keys=False, explicit_start=True) }}"
  when:
    - primary_rule.type == 'dict'
    - target_var_value is not none
    - target_var_value | length > 0

# Branch 2: Handle Single-Item-Lists (like controller_settings)
- name: "Process 'single_list_item' variable"
  ansible.builtin.set_fact:
    __files_to_create:
      - subfolder: "{{ (primary_rule.subfolder | default('') | trim | length > 0) | ternary(primary_rule.subfolder | safe_path_segment, '') }}"
        filename: "{{ primary_rule.filename | safe_path_segment }}"
        content: "{{ {target_var_name: target_var_value} | to_nice_yaml(indent=2, sort_keys=False, explicit_start=True) }}"
  when:
    - primary_rule.type == 'single_list_item'
    - target_var_value is not none
    - target_var_value is iterable and target_var_value is not string

# Branch 3: Handle normal Lists (all other vars)
- name: "Process 'list' variable"
  ansible.builtin.include_tasks: process_rule_group.yml
  loop: "{{ item_rules }}"
  loop_control:
    loop_var: rule
  when:
    - primary_rule.type == 'list'
    - target_var_value is not none
    - target_var_value is iterable and target_var_value is not string
    - rule.type == 'list' # Ensure we only run 'list' rules

- name: "Ensure base output directory exists for {{ target_folder_name }}"
  ansible.builtin.file:
    path: "{{ flat_to_filetree_output_path }}/{{ target_folder_name | safe_path_segment }}"
    state: directory
    mode: '0755'
  run_once: true
  delegate_to: localhost
  when:
    # - target_var_value is not none
    - __files_to_create is defined
    - __files_to_create | length > 0

- name: "Get unique list of subfolders"
  ansible.builtin.set_fact:
    __unique_subfolders: "{{ __files_to_create | map(attribute='subfolder') | list | unique }}"
  run_once: true
  delegate_to: localhost
  when:
    - __files_to_create is defined
    - __files_to_create | length > 0

- name: "Ensure subfolder directories exist"
  ansible.builtin.file:
    path: "{{ flat_to_filetree_output_path }}/{{ target_folder_name | safe_path_segment }}/{{ item | safe_path_segment }}"
    state: directory
    mode: '0755'
  loop: "{{ __unique_subfolders }}"
  loop_control:
    loop_var: item
    label: "Ensure subfolder directory exists for {{ item }}"
  run_once: true
  delegate_to: localhost
  when:
    - __unique_subfolders is defined
    - __unique_subfolders | length > 0
    - item is defined
    - item is not none
    - item | length > 0

# This task is the single point of file creation
- name: "📝 Create all files for '{{ target_var_name }}'"
  ansible.builtin.copy:
    content: "{{ item.content }}"
    dest: >-
      {{ flat_to_filetree_output_path | trim }}/{{ target_folder_name | safe_path_segment }}/{% if item.subfolder | default('') | trim | length > 0 %}{{ item.subfolder | safe_path_segment }}/{% endif %}{{ item.filename | safe_path_segment }}
    mode: '0644'
  loop: "{{ __files_to_create }}"
  loop_control:
    loop_var: item
    label: "{{ item.filename }}"
  run_once: true
  delegate_to: localhost
  when:
    - __files_to_create is defined
    - __files_to_create | length > 0
...
