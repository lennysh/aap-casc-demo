---
collections:
  - name: infra.aap_configuration

  - name: infra.aap_configuration_extended

  - name: ansible.controller
    version: '>=4.6,<4.7'

  - name: ansible.platform
    # version: '>=2.5,<2.6'
    version: '>=2.6,<2.7'  # 2.6+ currently needed because aap_configuration will try to include ansible.platform.role_team_assignment for 2.5

  - name: ansible.eda

  - name: ansible.hub
